name: Plugin Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install pytest
        run: pip install pytest

      - name: Run tests
        run: python3 -m pytest tests/ timecell-bitcoin/tests/ timecell-indian-mf/tests/ timecell-estate/tests/ -v --tb=short
